// RegisterPage.tsx
// This page renders the Register form for new user registration.
// It simply imports and displays the Register component.

import Register from '../components/Auth/Register';

// Functional component for the register page
const RegisterPage = () => <Register />;

// Export the RegisterPage component as default
export default RegisterPage;
